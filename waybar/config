{
    "layer": "top",
    "position": "top",
    "modules-left": ["hyprland/workspaces"],
    "modules-center": ["hyprland/window"],
    "modules-right": ["network", "bluetooth", "pulseaudio", "battery", "cpu", "memory", "clock"],
    "hyprland/window": {
        "max-length": 50
    },
    "clock": {
        "format": "{:%Y-%m-%d %H:%M:%S}"
    },
    "battery": {
		"format": "{capacity}% {icon}",
		"format-icons": ["", "", "", "", ""]
	},
    "cpu": {
        "format": "{usage}%"
    },
    "memory": {
        "format": "{used}/{total} GB"
    },
    "network": {
        "interface": "wlan0",
        "format": "{essid} {ip}",
        "format-disconnected": "No network",
        "format-alt": "{interface}"
    },
    "pulseaudio": {
        "format": "{volume}% {icon}",
        "format-muted": "Muted {icon}"
    },
    "bluetooth": {
        "format": "{icon}",
        "format-disconnected": "",
        "tooltip": false
    }
}
